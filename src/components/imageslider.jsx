import { useState } from "react";
const ImageSlider = ({ slides }) => {
    const [currentINdex, setCurrentUser] = useState(0);
    return (
        <div style={{ backgroumd }}>
            <div style={slideStyles}></div>
        </div>
    )
};

export default ImageSlider;


